<UserControl x:Class="LiteQASuite.Stub.StubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Brush.Background}">
    <StackPanel Margin="40" VerticalAlignment="Center" HorizontalAlignment="Left">
        <TextBlock Text="{Binding Title}"
                   Foreground="{DynamicResource Brush.Foreground}"
                   FontSize="22" FontWeight="SemiBold"/>
        <TextBlock Text="{Binding Greeting}"
                   Foreground="{DynamicResource Brush.ForegroundMuted}"
                   FontSize="15" Margin="0,10,0,0" TextWrapping="Wrap" MaxWidth="560"/>
    </StackPanel>
</UserControl>