<Window x:Class="LiteQASuite.Shell.Notifications.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False"
        ResizeMode="NoResize" SizeToContent="Height"
        Width="320" WindowStartupLocation="Manual" Opacity="0">
    <Border Background="{DynamicResource Brush.Surface}"
            BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1"
            CornerRadius="8" Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.35" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="AccentBar" Grid.Column="0"
                    Background="{DynamicResource Brush.Accent}" CornerRadius="8,0,0,8"/>
            <TextBlock x:Name="MessageText" Grid.Column="1"
                       Foreground="{DynamicResource Brush.Foreground}"
                       FontSize="14" TextWrapping="Wrap" Margin="14,12"/>
        </Grid>
    </Border>
</Window>